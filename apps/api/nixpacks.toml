[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --ignore-scripts --no-audit"]

[phases.build]
cmds = ["npm run build", "npx prisma generate"]

[start]
cmd = "node dist/app.js"
